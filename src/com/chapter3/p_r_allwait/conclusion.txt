定义:
	“假死”的现象其实就是线程进入WAITING等待状态。如果全部线程都进入WAITING状态，则程序就不再执行任何业务功能了，整个项目呈停止状态。
现象:
	 所有线程进行等待状态,并不是互相等锁的死锁状态。用jconsole和jps, jstack并不能检测出死锁。
造成原因：
	通过wait/notify进行通信了，但不保证notify唤醒的是异类，也许是同类，比如“生产者”唤醒“生产者”，或“消费者”唤醒“消费者”这样的情况。被唤醒的线程
	由于达不到执行条件，马上又进入了wait状态。
解决办法：
   将线程中的notify都改成notifyAll即可
